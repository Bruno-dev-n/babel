// "foreach" ele so intera, nao retorna nada.
// no "map" interar, fazer modificação e retorna.
// "find" procurar um item dentro de uma array
//"findidex" retorn o indice dentro da array
//"every "retona se todos os item tem a mesma condiçao
//"some" faz o mesmo porem ele precisa que so 1 item tenha a condiçao
//"filter" filtra item
//"reduce" é um somador tipo 'for'
